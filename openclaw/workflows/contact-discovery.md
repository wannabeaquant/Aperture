# contact-discovery

Return strict JSON with:

- `contacts`
- `website_urls`
- `confidence_notes`

Each contact must include:

- `channel`
- `value`
- `confidence`
- `public_business_contact`
- `whatsapp_likely`

Only return publicly visible business contacts.

