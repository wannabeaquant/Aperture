# OpenClaw Runtime Notes

This folder holds Aperture-specific OpenClaw configuration and workflow notes.

- `openclaw.json5.example` defines model aliases and agent defaults.
- Workflow prompts live under `workflows/`.
- Agent ids must line up with the environment variables used by the backend.

